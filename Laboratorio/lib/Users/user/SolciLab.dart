import 'package:flutter/material.dart';

class SolicitudLab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Lista de Solicitud', textScaleFactor: 2.2),
    );
  }
}
